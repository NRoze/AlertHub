# name: CD - Deploy on Tag

# on:
#   push:
#     tags:
#       - 'v*'

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       # 1️ Checkout repository
#       - uses: actions/checkout@v3

#       # 2️ Setup .NET 10
#       - name: Setup .NET
#         uses: actions/setup-dotnet@v3
#         with:
#           dotnet-version: '10.0.x'

#       # 3️ Azure Login using service principal
#       - name: Azure Login
#         uses: azure/login@v1
#         with:
#           creds: ${{ secrets.AZURE_CREDENTIALS }}

#       # ----- Function App Deployment -----
#       - name: Publish Function App
#         run: |
#           dotnet publish ./AlertHub.Api/AlertHub.Api.csproj \
#             -c Release \
#             -o $PWD/publish/FunctionApp \
#             /p:AppSettingsFile=appsettings.function.json

#       - name: Zip Function App Contents
#         run: |
#           cd ./publish/FunctionApp
#           zip -r ../../FunctionApp.zip .   # zip **contents** of folder
#           cd ../../

#       - name: Deploy Function App ZIP
#         run: |
#           az functionapp deploy \
#             --resource-group alert-hub-rg \
#             --name alert-hub-api \
#             --src-path FunctionApp.zip \
#             --type zip

#       # ----- WebApi Deployment -----
#       - name: Publish WebApi
#         run: |
#           dotnet publish ./AlertHub.WebApi/AlertHub.WebApi.csproj \
#             -c Release \
#             -o $PWD/publish/WebApi \
#             /p:AppSettingsFile=appsettings.webapi.json

#       - name: Zip WebApi Contents
#         run: |
#           cd ./publish/WebApi
#           zip -r ../../WebApi.zip .   # zip **contents** of folder
#           cd ../../

#       - name: Deploy WebApi ZIP
#         run: |
#           az webapp deploy \
#             --resource-group alert-hub-rg \
#             --name alert-hub-webapi \
#             --src-path WebApi.zip \
#             --type zip

#       - name: Set WebApi startup command
#         run: |
#           az webapp config set \
#             --resource-group alert-hub-rg \
#             --name alert-hub-webapi \
#             --startup-file "dotnet AlertHub.WebApi.dll"

#       # ----- SSE Endpoint Verification (optional) -----
#       # - name: Verify SSE Endpoint
#       #   run: |
#       #     URL="https://alert-hub-webapi.azurewebsites.net/api/alerts/sse"
#       #     HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" "$URL")
#       #     if [ "$HTTP_STATUS" -ne 200 ]; then
#       #       echo "❌ SSE endpoint check failed with status $HTTP_STATUS"
#       #       exit 1
#       #     fi
#       #     echo "✅ SSE endpoint is live!"

#       # ----- Optional Cleanup -----
#       - name: Cleanup build artifacts
#         run: rm -rf ./publish ./FunctionApp.zip ./WebApi.zip